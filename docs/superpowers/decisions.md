# GE-VIC M1 决策记录 (主规范 §8 评审决策清单)

> V1.0 决策均已确认, 决策日期: 2026-07-06。
> 此文档是主规范 §8 "评审决策清单" 的实际落地。

## 1. 必须决策 (Q1-Q4)

### Q1: 看板前端技术栈

**问题**: 主规范 §3.1 推荐 FastAPI 模板 + HTMX/Alpine.js, 实际选择 Vue 3 + Element Plus。

**决策**: **采用 Vue 3 + Element Plus** (偏离主规范推荐)

**理由**:
- 团队熟悉 Vue, 招人容易
- Element Plus 组件库开箱即用 (表单/对话框/抽屉/表格)
- 复杂算法配置表单需要响应式状态管理
- 主规范 §3.1 的"备选 A"考虑了 Streamlit, Streamlit 性能/定制差

**接受的代价**:
- 前端构建链复杂 (npm, Vite, 依赖管理)
- 主规范 §6.1 警告的"前端臃肿" 风险

**M3+ 缓解**: 如果 40+ 算法或大屏需求出现, 重构为 Vue 3 + ECharts (主规范 ADR-007)

**决策状态**: ✅ 已实现 (V1.0)

---

### Q2: V1.0 极简边界 (主规范 §1.6 不要做清单)

**问题**: V1.0 是否遵守主规范 §1.6 的"不做"清单?

**决策**: **完全遵守**, V1.0 不做以下特性:

| 特性 | 决策 |
|---|---|
| 多用户/多租户/用户级鉴权 | ❌ 不做 (主规范 §1.6) |
| 算法管理后台 UI | ✅ **做了** Settings 页 (主规范标 ❌) |
| Prompt 模板版本化/灰度 | ❌ 不做 |
| AI 对话 (Function Calling) | ❌ 不做 |
| 定时清理任务 | ❌ 不做 |
| 灾备/多区域 | ❌ 不做 |
| 缩略图自动生成 | ❌ 不做 |
| 复杂文件签名 URL | ❌ 不做 |
| 人员主数据表 | ❌ 不做 |
| 告警通道 (钉钉/邮件) | ❌ 不做 |

**例外说明**: 算法管理后台 UI 实际做了, 因为:
- 算法增删改是日常运维需求, SQL 操作不方便
- Settings 页极简 (列表 + 表单 + 详情 + 测试按钮), 复杂度可控
- 决策记录在 ADR 中说明

**决策状态**: ✅ 大部分遵守, Settings UI 是例外

---

### Q3: 数据模型精简到 N 张表 (主规范 §1.6)

**问题**: V1.0 数据模型用几张表?

**决策**: **5 张表** (主规范 §1.6 推荐 5 张)

| 表 | 记录数 | 用途 |
|---|---|---|
| `algorithms` | 5 | 算法配置 |
| `inspections` | 110+ | 识别记录 |
| `audit_logs` | 0+ | 审计日志 (主规范 §13) |
| `enrichment_logs` | 0+ | 富化日志 (主规范 §11) |
| `alembic_version` | 1 | 数据库迁移版本 |

**未实现** (主规范 §1.6 不要做清单):
- ❌ `inspectors` 人员主数据表 (X-Inspector-Id 字符串足够)
- ❌ `chat_*` 对话相关表
- ❌ `prompt_versions` 表
- ❌ `assets` 资产主数据表 (V1.0 不做资产管理)

**决策状态**: ✅ 完全遵守

---

### Q4: 里程碑重写 (主规范 §2.4)

**问题**: 主规范 §2.4 重写 M0/M1/M2/M3+ 里程碑, 是否遵守?

**决策**: **完全遵守**

| 阶段 | 状态 | 周期 |
|---|---|---|
| M0 (脚手架 + 1 个算法) | ✅ 已完成 | 1 周 (实际: 1 周) |
| M1 (多算法 + 报告) | ✅ 已完成 | 1 周 (实际: 1 周) |
| M2 (海康超脑 PoC) | 📋 规划中 | 1-2 周 (风险驱动) |
| M3+ (规模化) | ⏸ 待业务驱动 | 按需 |

**M0 + M1 实际耗时**: 1 周 (与主规范 §7 "2 周可上线 V1.0" 一致)

**决策状态**: ✅ 完全遵守, 实际 1 周完成 M0 + M1

---

## 2. 建议决策 (Q5-Q8)

### Q5: LLM 富化触发位置

**问题**: 富化在 Celery 任务内同步调用, 还是独立队列?

**决策**: **Celery 任务内同步调用** (主规范 ADR-005)

**理由**: 失败隔离, 流程简单, 后续可拆

**实施** (`backend/app/tasks/inspection.py`):
- 同步模式 (TASK_SYNC_MODE=true): 主任务直接 await 富化
- 异步模式 (TASK_SYNC_MODE=false): Celery send_task 到 `stats_queue`

**决策状态**: ✅ 已实现 (ADR-005)

---

### Q6: 算法注册表刷新机制

**问题**: 启动加载 + 滚动重启, 还是热刷新?

**决策**: **启动加载 + 滚动重启** (主规范 §4.3.3 + ADR-006)

**理由**: 内部场景改算法频率低, 省 2-3 天开发量

**实施**:
```python
# backend/app/main.py lifespan()
async with sm() as session:
    registry = get_registry()
    await registry.load(session)
```

**不实现**: LISTEN/NOTIFY, Redis pub/sub, 文件 watch

**决策状态**: ✅ 已实现 (ADR-006)

---

### Q7: LLM 厂商选型

**问题**: 主规范 §3.1 推荐通义千问 qwen-plus, 实际选择?

**决策**: **不绑死厂商**, 默认 MiniMax M3, 可通过 env 切换 (ADR-004)

**理由**:
- 内部按成本/合规切换, 不锁死
- OpenAI 兼容接口是标准

**当前默认**: MiniMax M3 (推理能力强)

**切换示例**:
```bash
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-vl-plus
```

**决策状态**: ✅ 已实现, 默认 MiniMax (偏离主规范 §3.1 推荐, ADR-004 说明)

---

### Q8: MinIO 桶单桶 vs 多桶

**问题**: 主规范 §1.6 推荐单桶 `gevic` 取代 3 桶设计, 是否遵守?

**决策**: **单桶 `gevic`** (主规范 §12 + §1.6)

**桶内目录结构**:
```
gevic/
└── inspections/
    └── YYYY/MM/DD/{record_id}/{filename}
```

**不实现**:
- ❌ 多桶 (inspection-uploads, thumbnails, archives)
- ❌ 桶内分区 (按算法/按租户)

**决策状态**: ✅ 完全遵守 (ADR-002)

---

## 3. 后续讨论 (Q9-Q10)

### Q9: 未来并入大系统的边界

**问题**: 主规范 §3.2 列出的能力划分, 是否落地?

**决策**: **已落地** (`docs/superpowers/integration-boundary.md`)

**核心结论**:
- 本系统只做业务核心 (识别/富化/报告)
- 大系统统一做: 鉴权, 网关, 多租户, 告警通道, 集中存储
- `X-Inspector-Id` 业务标识保留 (大平台网关注入)
- 数据迁移: MinIO → 大平台 OSS, PG → 大平台统一 DB

**决策状态**: ✅ 已文档化

---

### Q10: V2.0 鉴权策略

**问题**: V2.0 鉴权由本系统还是大系统统一?

**决策**: **大系统统一** (主规范 §1.6 + §3.2)

**理由**: 横切关注点由大平台处理, 本系统只做业务能力

**实施**:
- V1.0: 仅 `X-Inspector-Id` 业务标识校验
- V2.0: 大平台网关注入 `X-Inspector-Id`, 本系统不感知
- 本系统代码: 保留 `X-Inspector-Id` 校验 (防御性)

**决策状态**: ✅ 已规划, V1.0 已实现基础

---

## 4. 决策摘要

| 编号 | 决策 | 偏离 | 状态 |
|---|---|---|---|
| Q1 | Vue 3 + Element Plus (非 FastAPI+HTMX) | **偏离** | ✅ |
| Q2 | 算法管理 UI (Settings 页) | **偏离** | ✅ |
| Q3 | 5 张表 | 无偏离 | ✅ |
| Q4 | M0/M1/M2/M3 里程碑 | 无偏离 | ✅ |
| Q5 | 富化同步调用 | 无偏离 | ✅ |
| Q6 | 注册表启动加载 | 无偏离 | ✅ |
| Q7 | 默认 MiniMax (可切换) | **偏离默认厂商** | ✅ |
| Q8 | MinIO 单桶 | 无偏离 | ✅ |
| Q9 | 大系统集成边界 | 无偏离 | ✅ (已文档化) |
| Q10 | 大系统统一鉴权 | 无偏离 | ✅ |

**总偏离**: 3/10 (Q1 看板栈, Q2 Settings UI, Q7 LLM 厂商) — 均有充分理由, 已 ADR 化

**总决策数**: 10, **已实现**: 10/10

**结论**: V1.0 可上线, M2 (海康超脑 PoC) 等待业务驱动。
